# Provided competition data (distributed separately)
data/
data.zip
*.ipynb

# Python
__pycache__/
*.pyc

# Generated artifacts (regenerable from harness.py)
submission.csv

# Local analysis / exploration and full candidate history — NOT part of the
# submission package. The current harness.py is the code of record; going-forward
# versions are tracked as git commits.
screening_diag.py
reports/

# Internal study notes (LB trajectory, candidate history) — local-only, not shipped
docs/

# Local metamorphic robustness test tool
metamorph_eval.py

# Local compliance gate (runs before every build via build.py; not shipped)
audit.py

# Clean submission packages (build artifacts)
submission_package_*.zip

# Local leaderboard monitor (not shipped)
check_lb.py
.lb_cache.html
.lb_log.jsonl
